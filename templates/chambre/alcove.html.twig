{% extends 'base.html.twig' %}

{% block title %}{{ chambre.nom }}{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" type="text/css" href="/css/chambre.css">
{% endblock %}

{% block body %}
    {% include 'commun/menu.html.twig' %}
    <!--<div class="container police">
        <h2 class="titre">{{ chambre.nom }}</h2>
        <div class="row">
            <div class="col-lg-6 col-md-12 col-sm-12">
                <p>
                    {{ chambre.description }}
                </p>
                <div class="slides-container">
                    <div style="height: 500px;">
                        {% for photo in photos %}
                            <div class="slides-alcove"
                                 style="width: 100%; height: 100%; background-size: cover; background-image: url({{ photo.url }})">
                            </div>
                        {% endfor %}
                    </div>
                </div>
            </div>
            <div class="col-lg-6 col-md-12 col-sm-12">
                <div class="row">
                    <div class="col-md-6 col-lg-6 col-sm-6">
                        <h5 class="font-weight-bold titre-partie">Equipements</h5>
                        <ul class="fa-ul">
                            <li><span class="fa-li"><i class="fas fa-user"></i></span> Sèche cheveux</li>
                            <li><span class="fa-li"><i class="fas fa-shower"></i></span> Douche à l'italienne</li>
                            <li><span class="fa-li"><i class="fas fa-toilet"></i></span> WC privé</li>
                            <li><span class="fa-li"><i class="fas fa-wifi"></i></span> Wifi</li>
                            <li><span class="fa-li"><i class="fas fa-tv"></i></span> Télévison</li>
                            <li><span class="fa-li"><i class="fas fa-bed"></i></span> Lit double 2 x 90 x 200 cm</li>
                            <li><span class="fa-li"><i class="fas fa-thermometer-empty"></i></span> Mini réfrigérateur</li>
                        </ul>
                        <h5 class="font-weight-bold titre-partie">Sur demande</h5>
                        <ul class="fa-ul">
                            <li><span class="fa-li"><i class="fas fa-bed"></i></span> Lit bébé</li>
                        </ul>
                    </div>
                    <div class="col-md-6 col-lg-6 col-sm-6">
                        <h5 class="font-weight-bold titre-partie">Disponibilités</h5>
                        <div id="datePickerAlcove" style="font-size:15px;"></div>
                        <div class="d-flex mt-2">
                            <div class="reserve-block"></div>
                            <div class="ml-2 font-weight-bold">Non disponible</div>
                        </div>
                    </div>
                </div>
                <div class="row">
                    <div class="col-md-12 col-lg-12">
                        <h5 class="font-weight-bold titre-partie">Tarifs</h5>
                        <table class="mb-2">
                            <thead>
                            <tr>
                                <th>Nombres de nuits</th>
                                <th>2 nuits</th>
                                <th>4 nuits</th>
                            </tr>
                            </thead>
                            <tbody>
                            <tr>
                                <th>2 personnes</th>
                                <th>90 €</th>
                                <th>85 €</th>
                            </tr>
                            </tbody>
                        </table>
                        <p class="mb-0">Les prix indiqués sont par nuitée</p>
                        <p class="mb-0"> Petit-déjeuner compris</p>
                        <p><strong>Taxe de séjour :</strong> 0.80 euro / jour / adulte</p>
                    </div>

                </div>

            </div>
        </div>

    </div>-->
    <section class="section-chambre">
  <div class="chambre-texte">
    <div class="contenu">
      <h2 class="text-center">{{ chambre.nom }}</h2>
      <p>
         {{ chambre.description }}
      </p>

      <h3>Équipements</h3>
      <ul>
        <li>Sèche cheveux</li>
        <li>Douche à l’italienne</li>
        <li>WC privé</li>
        <li>Wifi</li>
        <li>Télévision</li>
        <li>Lit double (2x90x200cm)</li>
        <li>Mini réfrigérateur</li>
        <li>Lit bébé (sur demande)</li>
      </ul>

      <div class="points">
        {% for photo in photos %}
          <span></span>
        {% endfor %}
    </div>
    </div>
  </div>

  <div class="chambre-image">
     <div class="slides">
      {% for photo in photos %}
        <img src={{ photo.url }} alt="Chambre La Forge">
      {% endfor %}
  </div>
    <div class="degrade-gauche"></div>
  </div>

  <div class="chambre-aside">
    <div class="tarifs">
      <h3>Tarifs</h3>
      <table>
        <tr>
          <th class="text-right">Nombre de nuits</th>
          <th>2</th>
          <th>4</th>
        </tr>
        <tr>
          <td class="text-right">2 personnes</td>
          <td>95€</td>
          <td>90€</td>
        </tr>
      </table>
      <p class="details">
        Les prix indiqués sont par nuitée<br>
        Petit-déjeuner compris<br>
        <span class="taxe">Taxe de séjour :</span> 0,80€ / jour / adulte
      </p>
    </div>

    <div class="disponibilites">
      <h3>Disponibilités</h3>
      <div id="datePickerAlcove" style="font-size: 15px;"></div>
    </div>
  </div>
</section>


<div class="avis-client">
  <!--<button class="avis-nav prev">❮</button>-->

  <div class="avis-container">
    {% for avis in avisListe %}
      <div class="avis {% if loop.first %}active{% endif %}">
        <blockquote>
          <p style="color: #231f1c !important;">{{ avis.commentaire }}</p>
          <footer>{{ avis.nom }}, le {{ avis.date | date('d-m-Y') }}</footer>
        </blockquote>
      </div>
    {% endfor %}
  </div>

  <!-- <button class="avis-nav next">❯</button> -->
</div>

<script>
// Gestion du carrousel d'avis
const avisElements = document.querySelectorAll('.avis');
/*const prevBtn = document.querySelector('.avis-nav.prev');
const nextBtn = document.querySelector('.avis-nav.next');*/
let currentAvis = 0;

function showAvis(index) {
  avisElements.forEach((avis, i) => {
    avis.classList.toggle('active', i === index);
  });
}

/*prevBtn.addEventListener('click', () => {
  currentAvis = (currentAvis - 1 + avisElements.length) % avisElements.length;
  showAvis(currentAvis);
});

nextBtn.addEventListener('click', () => {
  currentAvis = (currentAvis + 1) % avisElements.length;
  showAvis(currentAvis);
});*/

// Optionnel : passage automatique toutes les 10s
setInterval(() => {
  currentAvis = (currentAvis + 1) % avisElements.length;
  showAvis(currentAvis);
}, 10000);




 const images = document.querySelectorAll('.chambre-image img');
const points = document.querySelectorAll('.points span');
let index = 0;

function showSlide(i) {
  images.forEach((img, idx) => img.classList.toggle('active', idx === i));
  points.forEach((pt, idx) => pt.classList.toggle('active', idx === i));
}

showSlide(0);

setInterval(() => {
  index = (index + 1) % images.length;
  showSlide(index);
}, 5000);
</script>

{% endblock %}
{% block javascripts %}
    <script>
        let reservationsObjet = {{ reservations | json_encode() | raw }};
        let reservations = [];
        for (let i = 0; i < reservationsObjet.length; i++) {
            reservations.push(reservationsObjet[i].date.date.substring(0, 10));
        }

        function formatDateYYYYMMDD(date) {
            var yyyy = date.getFullYear().toString();
            var mm = (date.getMonth()+1).toString();
            var dd  = date.getDate().toString();
            // CONVERT mm AND dd INTO chars
            var mmChars = mm.split('');
            var ddChars = dd.split('');
            // CONCAT THE STRINGS IN YYYY-MM-DD FORMAT
            return yyyy + '-' + (mmChars[1] ? mm : "0" + mmChars[0]) + '-' + (ddChars[1] ? dd : "0" + ddChars[0]);
        }

        $(document).ready(function () {
            $(function () {
                $("#datePickerAlcove").datepicker({
                    prevText: 'Préc',
                    nextText: 'Suiv',
                    currentText: 'Aujourd\'hui',
                    dateFormat: "yy-mm-dd",
                    monthNames: ['Janvier', 'Fevrier', 'Mars', 'Avril', 'Mai', 'Juin',
                        'Juillet', 'Aout', 'Septembre', 'Octobre', 'Novembre', 'Decembre'],
                    monthNamesShort: ['Jan', 'Fev', 'Mar', 'Avr', 'Mai', 'Jun',
                        'Jul', 'Aou', 'Sep', 'Oct', 'Nov', 'Dec'],
                    dayNames: ['Dimanche', 'Lundi', 'Mardi', 'Mercredi', 'Jeudi', 'Vendredi', 'Samedi'],
                    dayNamesShort: ['Dim', 'Lun', 'Mar', 'Mer', 'Jeu', 'Ven', 'Sam'],
                    dayNamesMin: ['Di', 'Lu', 'Ma', 'Me', 'Je', 'Ve', 'Sa'],
                    firstDay: 1,
                    beforeShowDay: function (dateObjet) {
                        let date = formatDateYYYYMMDD(dateObjet);
                        for (let i = 0; i < reservations.length; i++) {
                            if (reservations[i] === date) {
                                return [true, 'reserve'];
                            }
                        }
                        return [true, 'disponible'];
                    }
                });
            });
        });
    </script>

{% endblock %}
